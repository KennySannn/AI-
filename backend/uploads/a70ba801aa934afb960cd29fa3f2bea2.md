# 提示工程

提示词工程是通过设计输入文本来引导大模型输出预期结果的技术。

## Few-shot

在提示词中给出少量示例（few-shot）能显著提升输出质量与格式稳定性。

## 思维链

让模型一步步推理（chain-of-thought）可以提升复杂任务的准确率。# 提示工程

提示词工程是通过设计输入文本来引导大模型输出预期结果的技术。

## Few-shot

在提示词中给出少量示例（few-shot）能显著提升输出质量与格式稳定性。

## 思维链

让模型一步步推理（chain-of-thought）可以提升复杂任务的准确率。# 提示工程

提示词工程是通过设计输入文本来引导大模型输出预期结果的技术。

## Few-shot

在提示词中给出少量示例（few-shot）能显著提升输出质量与格式稳定性。

## 思维链

让模型一步步推理（chain-of-thought）可以提升复杂任务的准确率。# 提示工程

提示词工程是通过设计输入文本来引导大模型输出预期结果的技术。

## Few-shot

在提示词中给出少量示例（few-shot）能显著提升输出质量与格式稳定性。

## 思维链

让模型一步步推理（chain-of-thought）可以提升复杂任务的准确率。# 提示工程

提示词工程是通过设计输入文本来引导大模型输出预期结果的技术。

## Few-shot

在提示词中给出少量示例（few-shot）能显著提升输出质量与格式稳定性。

## 思维链

让模型一步步推理（chain-of-thought）可以提升复杂任务的准确率。# 提示工程

提示词工程是通过设计输入文本来引导大模型输出预期结果的技术。

## Few-shot

在提示词中给出少量示例（few-shot）能显著提升输出质量与格式稳定性。

## 思维链

让模型一步步推理（chain-of-thought）可以提升复杂任务的准确率。# 提示工程

提示词工程是通过设计输入文本来引导大模型输出预期结果的技术。

## Few-shot

在提示词中给出少量示例（few-shot）能显著提升输出质量与格式稳定性。

## 思维链

让模型一步步推理（chain-of-thought）可以提升复杂任务的准确率。# 提示工程

提示词工程是通过设计输入文本来引导大模型输出预期结果的技术。

## Few-shot

在提示词中给出少量示例（few-shot）能显著提升输出质量与格式稳定性。

## 思维链

让模型一步步推理（chain-of-thought）可以提升复杂任务的准确率。