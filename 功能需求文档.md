# AI学习助手 v1.0 —— 功能需求文档

> 版本：v1.0 ｜ 状态：需求冻结（用户确认前不变更） ｜ 范围：仅以下 6 个功能，禁止任何额外功能

## 一、功能拆解与优先级

优先级说明：**P0 = 必须先做（核心闭环依赖）**，P1 = 可以后做（体验增强，不影响核心流程）。

### 1. 记笔记
| # | 小功能点 | 优先级 | 说明 |
|---|---------|--------|------|
| 1.1 | 创建笔记（标题 + 正文 + 学习日期） | P0 | 学完当天课程后写 |
| 1.2 | 笔记列表 / 查看笔记详情 | P0 | 能找回自己写过的笔记 |
| 1.3 | 编辑笔记 | P1 | 修改已写内容 |
| 1.4 | 删除笔记 | P1 | 删除错误笔记 |

### 2. 传资料
| # | 小功能点 | 优先级 | 说明 |
|---|---------|--------|------|
| 2.1 | 上传 PDF / PPT / Word / Markdown / TXT 文件 | P0 | 校验文件类型与大小（2026-09-15 用户变更：新增 Word、TXT） |
| 2.2 | 文档解析与切分（提取文本 → 分块入库，不使用向量模型） | P0 | 聊资料、做测评的数据基础（2026-09-15 变更：不做 RAG，聊资料时将所选资料文本直接提供给 LLM） |
| 2.3 | 资料列表（查看已上传资料及解析状态） | P0 | 知道哪些资料可用 |
| 2.4 | 删除资料（连带删除其分块与向量） | P1 | 清理不想要的资料 |
| 2.5 | 解析失败的资料重试解析 | P0 | 2026-09-15 用户变更：失败行提供重试按钮 |

### 3. 聊资料
| # | 小功能点 | 优先级 | 说明 |
|---|---------|--------|------|
| 3.1 | 提问 → 检索资料 → 生成回答（附引用来源） | P0 | 核心功能，回答必须优先基于资料 |
| 3.2 | 会话管理（新建会话 / 查看历史消息） | P1 | 支持多轮连续对话 |

### 4. 做测评
| # | 小功能点 | 优先级 | 说明 |
|---|---------|--------|------|
| 4.1 | 自动出题：基于所选资料出题（单选 + 判断 + 简答三种题型） | P0 | 严格基于资料，不超纲 |
| 4.2 | 提交答卷 → 自动改卷 | P0 | 选择/判断自动判分，简答按 0/0.5/1 评分 |
| 4.3 | 答题结果落库（每题得分、知识点归属） | P0 | 看进度、出周报的数据来源 |
| 4.4 | 查看历史测验记录与错题 | P1 | 复盘用 |

### 5. 看进度
| # | 小功能点 | 优先级 | 说明 |
|---|---------|--------|------|
| 5.1 | 学习概览（笔记数、资料数、测验次数、平均得分） | P0 | 数据全部来自以上功能的落库结果 |
| 5.2 | 知识点掌握情况（各知识点的答题正确率 → 掌握/薄弱标记） | P0 | 直接回答"哪些知识点没掌握" |
| 5.3 | 资料笔记：每份资料可写一份笔记，输入后自动保存（无需手动保存）；长内容支持折叠 | P0 | 2026-09-15 用户变更新增 |

### 6. 出周报
| # | 小功能点 | 优先级 | 说明 |
|---|---------|--------|------|
| 6.1 | 生成周报：汇总指定一周的笔记、资料、答题、知识点掌握情况 | P0 | 依赖 1.3 / 2.3 / 4.3 / 5.2 的数据 |
| 6.2 | 历史周报查看 | P1 | 回看往期周报 |

**依赖关系**：传资料(2.2) → 聊资料(3.1)、做测评(4.1)；做测评(4.3) → 看进度(5.2)；全部落库数据 → 出周报(6.1)。

---

## 二、后台接口设计

统一约定：前缀 `/api`，请求/响应均为 JSON（上传接口除外）；响应统一包裹为 `{ "code": 0, "msg": "ok", "data": ... }`，出错时 `code != 0`。单用户系统，暂不做登录鉴权。

### 记笔记
| 方法 | 地址 | 入参 | 返回 |
|------|------|------|------|
| POST | `/api/notes` | `{ "title": string, "content": string, "study_date": "YYYY-MM-DD" }` | `{ "id": number }` |
| GET | `/api/notes` | Query：`page`, `page_size`, `keyword`(可选) | `{ "total": number, "list": [{ "id", "title", "study_date", "created_at" }] }` |
| GET | `/api/notes/:id` | 路径参数 id | `{ "id", "title", "content", "study_date", "created_at", "updated_at" }` |
| PUT | `/api/notes/:id` | `{ "title"?, "content"?, "study_date"? }`（均可选） | `{ "id": number }` |
| DELETE | `/api/notes/:id` | 路径参数 id | `{}` |

### 传资料
| 方法 | 地址 | 入参 | 返回 |
|------|------|------|------|
| POST | `/api/materials/upload` | `multipart/form-data`：`file`（仅 .pdf / .ppt / .pptx / .doc / .docx / .md / .txt） | `{ "id": number, "filename": string, "status": "parsing" }` |
| GET | `/api/materials` | Query：`page`, `page_size` | `{ "total": number, "list": [{ "id", "filename", "file_type", "size", "status", "chunk_count", "uploaded_at" }] }` |
| GET | `/api/materials/:id` | 路径参数 id | `{ "id", "filename", "file_type", "size", "status", "chunk_count", "uploaded_at" }` |
| POST | `/api/materials/:id/retry` | 路径参数 id（status=failed 时调用，重新触发解析） | `{ "id": number, "status": "parsing" }` |
| GET | `/api/materials/:id/note` | 路径参数 id | `{ "note_id": number\|null, "content": string\|null, "updated_at": string\|null }` |
| PUT | `/api/materials/:id/note` | `{ "content": string }`（自动保存调用，upsert） | `{ "note_id": number, "updated_at": string }` |
| DELETE | `/api/materials/:id` | 路径参数 id | `{}` |

`status` 取值：`parsing` 解析中 / `ready` 可用 / `failed` 失败。

### 聊资料
| 方法 | 地址 | 入参 | 返回 |
|------|------|------|------|
| POST | `/api/chat` | `{ "question": string, "material_ids": number[], "session_id": number? }`（无 session_id 则新建会话） | `{ "session_id": number, "answer": string, "citations": [{ "material_id", "filename", "chunk_id", "snippet" }] }` |
| POST | `/api/chat/sessions` | `{ "title"?: string }`（可省，默认"新对话"；2026-09-15 用户新增接口） | `{ "id": number, "title": string }` |
| GET | `/api/chat/sessions` | Query：`page`, `page_size` | `{ "total", "list": [{ "id", "title", "created_at" }] }` |
| DELETE | `/api/chat/sessions/:id` | 路径参数 id（连带删除全部消息；2026-09-15 用户新增接口） | `{}` |
| GET | `/api/chat/sessions/:id/messages` | 路径参数 id | `{ "list": [{ "role": "user"\|"assistant", "content", "citations", "created_at" }] }` |

### 做测评
| 方法 | 地址 | 入参 | 返回 |
|------|------|------|------|
| POST | `/api/quizzes/generate` | `{ "material_ids": number[], "choice_count": number, "judge_count": number, "short_count": number }` | `{ "quiz_id": number, "questions": [{ "question_id", "type": "choice"\|"judge"\|"short", "question", "options": string[]?, "score": number, "difficulty": "简单"\|"中等"\|"较难" }] }`（出题时不返回答案） |
| POST | `/api/quizzes/:id/submit` | `{ "answers": [{ "question_id": number, "answer": string }], "duration_seconds": number }` | `{ "total_score": number, "full_score": number, "results": [{ "question_id", "got_score": 0\|0.5\|1, "is_correct": boolean, "comment": string }] }` |
| GET | `/api/quizzes/:id` | 路径参数 id | `{ "quiz_id", "created_at", "duration_seconds": number, "total_score": number, "full_score": number, "questions": [{ "question_id", "type", "question", "options", "score", "answer", "difficulty" }], "results": [{ "question_id", "user_answer", "got_score", "is_correct", "comment" }] }`（total_score/full_score 为 2026-09-15 联调补充，前端结果页需要） |
| GET | `/api/quizzes` | Query：`page`, `page_size` | `{ "total", "list": [{ "quiz_id", "total_score", "full_score", "duration_seconds", "created_at" }] }` |

### 看进度
| 方法 | 地址 | 入参 | 返回 |
|------|------|------|------|
| GET | `/api/progress/overview` | 无 | `{ "note_count", "material_count", "quiz_count", "avg_score_rate" }` |
| GET | `/api/progress/knowledge-points` | 无 | `{ "list": [{ "name", "quiz_count", "correct_count", "correct_rate", "mastery": "mastered"\|"weak"\|"unknown" }] }` |

掌握规则：`correct_rate >= 0.8` → mastered；`< 0.6` → weak；其余 → unknown；从未被考过 → unknown。

### 出周报
| 方法 | 地址 | 入参 | 返回 |
|------|------|------|------|
| POST | `/api/reports/weekly/generate` | `{ "week_start": "YYYY-MM-DD", "week_end": "YYYY-MM-DD"? }`（week_end 可选，默认 start+6 天；2026-09-15 用户变更：支持自定义日期范围） | `{ "report_id": number }` |
| GET | `/api/reports/weekly` | Query：`page`, `page_size` | `{ "total", "list": [{ "id", "week_start", "week_end", "created_at" }] }` |
| GET | `/api/reports/weekly/:id` | 路径参数 id | `{ "id", "week_start", "week_end", "content": string, "stats": { "notes_written", "materials_uploaded", "quizzes_taken", "avg_score_rate", "weak_points": string[] } }` |

---

## 三、数据库设计

数据库类型：MySQL（分块向量可选 PostgreSQL + pgvector / 独立向量库，实施时定）。单用户系统，不设用户表。

### 1. materials（资料表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK 自增 | |
| filename | VARCHAR(255) | 原始文件名 |
| file_type | VARCHAR(10) | pdf / ppt / pptx / doc / docx / md / txt |
| file_path | VARCHAR(500) | 存储路径 |
| size | BIGINT | 字节数 |
| status | TINYINT | 0=解析中 1=可用 2=失败 |
| chunk_count | INT | 分块数量 |
| uploaded_at | DATETIME | 上传时间 |

### 2. chunks（资料分块表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK 自增 | |
| material_id | BIGINT, FK → materials.id, 级联删除 | |
| chunk_index | INT | 块序号 |
| content | TEXT | 分块文本 |
| embedding | VECTOR / BLOB | 向量（或存独立向量库，此处存引用 ID） |

索引：`idx_material (material_id)`。

### 3. chat_sessions（聊天会话表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK 自增 | |
| title | VARCHAR(255) | 默认取首条提问截断 |
| created_at | DATETIME | |

### 4. chat_messages（聊天消息表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK 自增 | |
| session_id | BIGINT, FK → chat_sessions.id, 级联删除 | |
| role | VARCHAR(10) | user / assistant |
| content | TEXT | 消息内容 |
| citations | JSON | 引用来源：`[{material_id, filename, chunk_id, snippet}]` |
| created_at | DATETIME | |

索引：`idx_session (session_id)`。

### 5. notes（笔记表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK 自增 | |
| title | VARCHAR(255) | |
| content | TEXT | Markdown 正文 |
| material_id | BIGINT NULL, FK → materials.id | 资料笔记关联的资料（独立笔记为 NULL，2026-09-15 用户变更新增） |
| study_date | DATE | 学习日期 |
| created_at / updated_at | DATETIME | |

索引：`idx_study_date (study_date)`（周报按周统计用）。

### 6. quizzes（测验表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK 自增 | |
| material_ids | JSON | 出题依据的资料 ID 列表 |
| status | TINYINT | 0=未作答 1=已完成 |
| total_score / full_score | DECIMAL(5,1) | 得分 / 满分 |
| duration_seconds | INT | 答题用时（秒）（2026-09-15 用户变更新增） |
| created_at | DATETIME | |

### 7. quiz_questions（题目表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK 自增 | |
| quiz_id | BIGINT, FK → quizzes.id, 级联删除 | |
| type | VARCHAR(10) | choice / judge / short |
| question | TEXT | 题干 |
| options | JSON | 选择题选项，判/简答为 NULL |
| answer | TEXT | 标准答案（选择题存正确选项号，简答存参考答案要点） |
| score | DECIMAL(3,1) | 分值，默认：选择/判断 1 分，简答 1 分 |
| difficulty | VARCHAR(10) | 难度：简单 / 中等 / 较难（出题时由 AI 标注，2026-09-15 用户变更新增） |
| knowledge_point | VARCHAR(100) | 知识点归属（出题时由 AI 标注） |

索引：`idx_quiz (quiz_id)`、`idx_kp (knowledge_point)`。

### 8. quiz_answers（作答记录表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK 自增 | |
| question_id | BIGINT, FK → quiz_questions.id | |
| quiz_id | BIGINT, FK → quizzes.id | 冗余，便于按测验统计 |
| user_answer | TEXT | 用户作答 |
| got_score | DECIMAL(3,1) | 得分：选择/判断 0 或 1；简答 0 / 0.5 / 1 |
| is_correct | TINYINT | 是否答对 |
| comment | TEXT | AI 简短点评（简答题用） |
| answered_at | DATETIME | |

索引：`idx_question (question_id)`、`idx_quiz (quiz_id)`。

### 9. knowledge_mastery（知识点掌握表，由答题结果聚合）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK 自增 | |
| name | VARCHAR(100) UNIQUE | 知识点名 |
| quiz_count | INT | 被考次数 |
| correct_count | INT | 答对次数 |
| correct_rate | DECIMAL(4,3) | 正确率 |
| mastery | VARCHAR(10) | mastered / weak / unknown |
| updated_at | DATETIME | |

### 10. weekly_reports（周报表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK 自增 | |
| week_start / week_end | DATE | 统计区间（7 天） |
| content | TEXT | AI 生成的周报正文（Markdown） |
| stats | JSON | 结构化统计：`{notes_written, materials_uploaded, quizzes_taken, avg_score_rate, weak_points[]}` |
| created_at | DATETIME | |

索引：`idx_week (week_start)`。

---

## 四、AI 系统提示词（三套"人设"）

以下为正式指令，运行时作为 system prompt 注入，`{{ }}` 为运行时填充变量。

### 提示词一：日常聊天 —— AI大模型学习导师

```
你是「AI大模型学习导师」，服务对象是一位正在学习大模型课程、持有大量 PDF/PPT/Markdown 资料的学员。

【身份与风格】
1. 你是 AI 大模型领域的学习导师：有洞察、不啰嗦。回答直击要点，先给结论，再给必要解释。
2. 不堆砌术语，不写空洞客套话。能用一句话说清的，不用三句。
3. 学员提出的问题若超出其当前学习阶段，给出一句话点拨即可，不主动展开超纲内容。

【资料优先原则】
4. 回答优先依据 <资料上下文> 中的内容（由系统检索注入）：
   - 资料中有答案：严格基于资料回答，并在结尾以「来源：文件名」标注引用的资料。
   - 资料中没有答案：先用一句话声明"资料中未涉及"，再以你的专业知识补充回答，并明确区分"资料观点"与"补充观点"。
5. 引用资料时不得篡改、夸大或曲解原文含义；资料内容与你自身知识冲突时，以资料为准并提示学员可能存在版本差异。
6. 多轮对话时结合 <历史对话> 保持连贯，但每次回答仍以资料检索结果为第一依据。

【输出格式】
7. 默认使用简体中文；代码、术语首次出现可附英文原词。
8. 回答结构：结论 → 解释/示例（必要时）→ 来源标注。长度以说清为限，禁止无意义扩写。
```

### 提示词二：出题 —— 基于资料的出题官

```
你是「出题官」，任务是基于学员提供的课程资料生成测验题，用于检验学员对资料内容的掌握程度。

【出题原则】
1. 题目必须严格依据 <资料上下文> 中的原文内容出题，禁止出资料中不存在或无法从资料推断的知识点。
2. 每道题必须标注其内容来源对应的资料，并归纳一个 knowledge_point（知识点名，简短、可聚合统计）。
3. 每道题必须标注 difficulty（难度）：简单 / 中等 / 较难，依据资料内容的理解门槛评定。
4. 题目考察理解与应用，避免无意义的原文背诵题；题干表述清晰、无歧义，答案在资料中有明确依据。

【题型与数量】
按系统传入的数量要求生成三种题型：
5. 单选题（choice）：4 个选项（A/B/C/D），仅 1 个正确答案，干扰项须有迷惑性但明确错误。
6. 判断题（judge）：题干 + 对/错两选，正确答案为「对」或「错」。
7. 简答题（short）：题干开放但答案可依据资料评分；同时给出标准答案要点（3~5 条关键采分点），供改卷使用。

【评分标准】
8. 每题分值均为 1 分：单选/判断答对得 1 分、答错得 0 分；简答题按采分点覆盖度给 0 / 0.5 / 1 分。
9. 全卷满分 = 题目数量，不做附加分。

【输出格式】
10. 仅输出如下 JSON，不输出任何其他文字：
{
  "questions": [
    {
      "type": "choice",            // choice | judge | short
      "question": "题干",
      "options": ["A. ...", "B. ...", "C. ...", "D. ..."],  // 仅 choice 有；judge/short 为 null
      "answer": "A",               // choice 存选项字母；judge 存 "对" 或 "错"；short 存标准答案要点
      "difficulty": "简单",         // 简单 | 中等 | 较难
      "knowledge_point": "知识点名",
      "source_material": "资料文件名"
    }
  ]
}
```

### 提示词三：改卷 —— 客观评分员

```
你是「评分员」，任务是对学员的测验答卷评分。评分唯一依据：题目、标准答案、学员作答。禁止参考其他信息，禁止主观加减分。

【客观题评分规则】
1. 单选题（choice）：学员答案与标准答案的选项字母完全一致 → 1 分；否则 0 分。不解释、不变通。
2. 判断题（judge）：学员答案与标准答案完全一致（"对"/"错"）→ 1 分；否则 0 分。

【简答题评分规则】（每题 0 / 0.5 / 1 三档，无其他分值）
3. 对照标准答案中的采分点逐条比对学员作答：
   - 1 分：覆盖全部或绝大多数关键采分点，表述正确。
   - 0.5 分：覆盖约一半关键采分点，或覆盖多数但有明显错误表述。
   - 0 分：未覆盖关键采分点，答非所问，或表述完全错误。
4. 允许同义表述：学员用自己的话正确表达同一含义，视为覆盖该采分点。
5. 学员答案中出现自相矛盾的内容时，按错误处理，不取其一。

【输出格式】
6. 每题输出得分，简答题附一句不超过 30 字的点评（指出缺了什么采分点或错在哪）。仅输出如下 JSON：
{
  "results": [
    {
      "question_id": 题目ID,
      "got_score": 1,            // 选择/判断：0 或 1；简答：0 / 0.5 / 1
      "is_correct": true,        // 得分等于该题满分即为 true
      "comment": ""              // 仅简答题需要，客观题为空字符串
    }
  ]
}
7. 不输出评分以外的任何建议、鼓励或总结，那是其他模块的职责。
```

---

## 五、范围边界（冻结）

- 本文档之外的功能一律不做；任何变更须由用户主动提出并更新本文档。
- 单用户、无登录、无多端同步；不做移动端适配要求。
- 支持文件类型仅限 PDF / PPT / Word / Markdown / TXT（2026-09-15 用户变更，新增 Word、TXT）。
